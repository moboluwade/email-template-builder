# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log

# === Yarn Berry ===
.pnp.*
.yarn/cache/*
.yarn/unplugged/*
.yarn/build-state.yml
.yarn/install-state.gz

# Keep these parts of .yarn/ (important for reproducibility)
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# === Storybook ===
# Keep .storybook/ (do NOT ignore)
# Ignore Storybook build output (default folder is 'storybook-static')
storybook-static/

# === Node ===
node_modules/

